import {
	getPrivateUserInfoFactory,
	getPublicUserInfoFactory,
} from '../../../use-cases/queries';
import type {
	FullUser,
	PartialUser,
} from '../../../use-cases/queries/read-models/Index';
import { queriesRepository } from '../../../infra/queries-repository/Repository';

export interface QueriesServices {
	// methods will be added here
}

export const queriesServices: QueriesServices = {
	// factory bindings will be added here
};
