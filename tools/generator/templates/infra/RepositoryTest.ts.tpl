import { it, expect, describe, beforeEach, afterAll } from 'bun:test';
import { {{RepositoryVariableName}} } from './repository';
import { prisma } from '@PrismaClient';

const {
} = {{RepositoryVariableName}};


beforeEach(async () => {
});

afterAll(async () => {
});

describe('Repository', () => {
	describe('TODO', () => {

	});
});
